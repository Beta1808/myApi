# Минимальный workflow
name: CI
on: [push]
jobs:
  test:
    runs-on: self-hosted  # Ваш легковесный сервер
    steps:
      - uses: actions/checkout@v4
      - run: |
          pip install pytest
          pytest tests/